# A self-test fixture of Build/CheckProjectFiles.py: the alternation omits Names, so that project's headers would go unchecked.
HeaderFilterRegex: '(Clean|Shape|Registry|Moved|[A-Za-z0-9]+Tests)[/\\][A-Za-z0-9]+\.h$'
